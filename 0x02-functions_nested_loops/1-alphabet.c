#include "main.h"
/**
 * print_alphabet - alphabet in lowercase
 *
 * Return: Always 0
 */
print_alphabet(void)
{
	int char;

	for (char = 'a'; char < = 'z'; char++)
	{
		putchar("ch");
	}
	putchar("\n");
	return (0);
}
